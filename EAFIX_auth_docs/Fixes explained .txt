Strong revision. It does what I asked — turns the prose into a real execution spec with task IDs, a dependency DAG, acceptance criteria, blocking conditions, and a definition of done. All six gaps from last round are addressed: tiered missing-behavior (T08), applicability matrix keyed on module_kind/deployable_scope (T06), module_catalog demoted to conflict-check for process binding (T07), file-ownership policy with ownership classes (T04/T05), regeneration policy with preserve_manual (T09), and the document_type patch (T01). I verified the load-bearing assumptions against the actual files. Most hold. Three don’t, and they matter.

Factual errors (verified against the source files)

	1.	The shared-entrypoint rule invents a module owner. T05 and the T11 fixture list F3_CLOCK_SCHEDULER among the modules sharing services/calendar-ingestor/src/2099900093260118_main.py. The actual file_module_mapping.csv row lists only D2, D3, F2, D4 — not F3. F3_CLOCK_SCHEDULER is step S02 (scheduler.py), a different file entirely. So the control layer built to stop guessing is itself guessing a file owner. It’s also internally inconsistent: T05/T11 say five modules, T14 BATCH_B says four. Drop F3 from allowed_modules, canonical_modules_contains_any, and the fixture, or the shared-entrypoint regression test encodes a false expectation.
	2.	Three named primary/fallback sources don’t exist in project knowledge. communication_channels.json (primary for the comms domain), mt4 authoritative reference for ai.json, and 2026012120420004_MODULE_ASSIGNMENT_POLICY.yaml are all absent. The real comms artifact is MT4_Python_communication_channels.txt (plus channel data embedded in module_catalog.json and ui_catalog.json). The validator’s pass list (T10) has no source-existence check, and the global “do_not_generate until…” gate won’t catch a dangling authority reference — so the pipeline can run green while pointing at phantom files. Add source_existence_validate to T10 and repoint these three entries.
	3.	Identity authority re-encodes the very drift it’s resolving. source_authority_order makes module_catalog.json primary for module_identity_and_purpose but the Module Map .txt primary for numeric_module_identity. These overlap. module_catalog.json already carries the 20-digit IDs (50000000000000000001…), so it should be the single numeric-ID authority with the map as fallback only. As written, two documents both claim primary for the numeric ID.

What’s correctly grounded, so don’t touch it: the schema genuinely supports the ownership fix (owned_files, shared_files, unassigned_candidate_files, file_assignment_status, ownership_derivation, file_assignment_policy_ref all exist; moduleScope.forbidden_responsibilities exists). The D2 fixture is exact — S03, step 3, calendar_service.py::poll_scheduler, inputs CalendarSourceConfig+ScheduleTick, output CalendarRaw all match process_step_catalog.json. And updated_trading_process_aligned.json does carry phases/phase_id, so T07’s phase augmentation is sourceable.

Structural / DAG issues

	4.	T13 under-depends. It depends on T01, T02, T04, T10, T11 — but not T05, T06, T07, T08, or T09. So the harness and fixtures can run before the shared-calendar rule (T05), process-binding rule (T07), tiered behavior (T08), and regen policies (T09) exist. The fixtures test exactly those behaviors. Add T05–T09 to T13’s depends_on, and add T05/T07/T09 to T10’s (it currently lists only T02/T04/T06/T08).
	5.	No full-corpus duplicate sweep. FAIL_DUPLICATE_OWNED_FILE is correctly global (“more than one manifest”), but T14 only validates “each batch before the next starts,” and T10’s input is a single manifest or directory. A file owned in BATCH_B and again in BATCH_C won’t surface until something validates the whole set at once. Insert a corpus-wide duplicate-owned-file pass between T14 and T15.
	6.	Two promotion gates are currently unsatisfiable. Nothing computes staleness_policy.source_hashes (schema wants ^[a-fA-F0-9]{64}$) or sets staleness_status, yet T15 requires staleness_status_not_stale. With no hash step, status defaults to unknown and either blocks every promotion or is treated as vacuous. Assign hash computation to a task (natural fit: harness recompute_derived). Same gap for provenance: provenance_writeback is a field-rule key and T10 has source_provenance_validate, but no task specifies how per-field provenance lands in source_authority / staleness_policy.last_generated_from.
	7.	transform / validate_against are declared but never wired. T02 requires them per field-rule and I flagged pattern-validation last round, but no acceptance criterion forces them to carry the actual $defs regexes (module\_id ^5\d{19}$, step_code, symbol, contractName). They’ll exist as empty keys. Add to T03’s acceptance: every identity/contract field rule pins its validate_against to the schema pattern, and T10 does per-field pattern pre-validation, not just whole-manifest schema validation.

Minor: owned_files is schema-required, so a module whose only file is the shared entrypoint will have owned_files: [] with everything in shared_files. Confirm the no-duplicate validator treats empty owned_files as valid rather than flagging it.

Net: the architecture of the plan is right and ready once the three factual corrections (F3, phantom sources, identity authority) and the DAG fixes (T13/T10 deps, corpus sweep, hash/staleness owner) are applied. Those are edits to the spec, not a redesign.

Want me to produce a corrected source_authority_order (repointed to real files) and a patched task DAG with the dependency and sweep fixes applied, as a drop-in replacement block?