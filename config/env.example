# Example environment configuration for the local drop server
# Save as .env.local (or set as system env vars)

# REQUIRED: random secret used as Bearer token
DROP_TOKEN=replace-with-a-long-random-string

# Where to save incoming files locally
SAVE_BASE_DIR=C:\AI\agent_out

# Glob allow-list
ALLOW_PATTERNS=src/**,tests/**,.ai/**

# Optional: mirror into a local repo clone and push
REPO_DIR=C:\AI\repo
DEFAULT_BRANCH=main
ALLOWED_BRANCH_PREFIXES=lane/,feature/,hotfix/,bugfix/
PROTECTED_BRANCHES=main,master,develop
GIT_USER_NAME=Agent Bot
GIT_USER_EMAIL=agent@example.local

# Optional: disable git even if REPO_DIR set
# DISABLE_GIT=1

# Dev-only: allow localhost without token
# ALLOW_LOCALHOST_NO_TOKEN=1
