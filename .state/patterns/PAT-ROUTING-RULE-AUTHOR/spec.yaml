pattern_id: PAT-ROUTING-RULE-AUTHOR
version: 1.0.0
purpose: "Author routing rules from catalog-backed work-cell metadata."
executor_id: EXEC-ROUTING-RULE-WRITER
output_contract_ref: OUT-ROUTING-RULE
determinism:
  - stable_input_order
  - schema_backed_defaults_only
forbidden:
  - modify_protected_path
  - write_outside_declared_scope
