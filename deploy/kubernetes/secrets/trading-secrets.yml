---
# This is a template file - actual secrets should be managed via sealed-secrets or external secret operators
# DO NOT commit actual secret values to git
apiVersion: v1
kind: Secret
metadata:
  name: trading-secrets
  namespace: eafix-trading
  labels:
    app.kubernetes.io/name: eafix-trading-system
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Database credentials
  POSTGRES_USER: "placeholder_username"
  POSTGRES_PASSWORD: "placeholder_password"
  POSTGRES_DB: "eafix_trading"
  
  # Redis credentials
  REDIS_PASSWORD: "placeholder_redis_password"
  
  # External API keys (examples)
  MT4_API_KEY: "placeholder_mt4_key"
  BROKER_API_SECRET: "placeholder_broker_secret"
  
  # Service-to-service authentication
  JWT_SECRET_KEY: "placeholder_jwt_secret"
  
  # Monitoring credentials
  PROMETHEUS_PASSWORD: "placeholder_prometheus_password"
  GRAFANA_ADMIN_PASSWORD: "placeholder_grafana_password"
---
apiVersion: v1
kind: Secret
metadata:
  name: compliance-secrets
  namespace: eafix-trading
  labels:
    app.kubernetes.io/name: compliance-monitor
    app.kubernetes.io/component: compliance
type: Opaque
stringData:
  # Alertmanager webhook secrets
  SLACK_WEBHOOK_URL: "placeholder_slack_webhook"
  PAGERDUTY_SERVICE_KEY: "placeholder_pagerduty_key"
  SMTP_PASSWORD: "placeholder_smtp_password"
  
  # Regulatory reporting credentials
  CFTC_REPORTING_KEY: "placeholder_cftc_key"
  SEC_REPORTING_TOKEN: "placeholder_sec_token"
  FINRA_API_SECRET: "placeholder_finra_secret"